import { StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    datePickerButton: {
        padding: 15,
        borderWidth: 0.5,
        borderColor: 'gray',
        borderRadius: 8,
        backgroundColor: '#FFFFFF',
        height: 50,
        marginTop: 12,
      },
      dateText: {
        fontSize: 16,
        color: 'black',
      },
});